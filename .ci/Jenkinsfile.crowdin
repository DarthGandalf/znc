#!groovy

timestamps {
  node {
    timeout(time: 30, unit: 'MINUTES') {
      def wsdir = pwd()
      stage('Checkout') {
        shell "echo $wsdir"
        shell 'ls -l'
      }
    }
  }
}

// vim: set ts=2 sw=2 et:
